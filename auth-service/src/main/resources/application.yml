server:
  port: 8082

spring:
    datasource:
      url: jdbc:mysql://localhost:3306/auth_db
      username: root
      password: mysql@123

    jpa:
      hibernate:
        ddl-auto: update
    application:
      name: AUTH-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true